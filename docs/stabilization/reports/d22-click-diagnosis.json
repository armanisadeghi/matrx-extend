{
  "schema_version": 1,
  "report_id": "d22-click-diagnosis",
  "defect_id": "EXT-D-0022",
  "verdict": "Save pointer cause not yet established; a blocked hit target is confirmed",
  "evidence": [
    "Run 001 reached one_save_click after capture and a durable one-attempt reservation, then the trusted driver threw. That run did not retain the pointer phase or error category, so a Save dispatch cannot be inferred or ruled out.",
    "Run 002 used the existing checkpoint and real admin UI to obtain one scoped recognition lookup with result none. It stopped before Save. The checkpoint was neither reset nor deleted.",
    "Run 003 captured the same public page and found one visible, enabled Save button in the active Scrape pane, with nonzero target and clipped area. Its center and all nine interior points failed the hit test. No Save input was sent.",
    "The trusted driver resolves exact visible button text globally, scrolls the unique target, requires a hit-tested interior point, and dispatches press and release only after stable samples. Run 003 identifies a blocked hit target at observation time; run 001 did not record enough detail to prove the same cause."
  ],
  "change": "The read-only probe now adds only viewport and target geometry, the covering element's allowlisted tag, role, position and rectangle, and an ignored private 0600 screenshot. It still exits diagnostic_only without Save input or checkpoint changes.",
  "next_step": "Root runs the guarded diagnostic branch once more and inspects the structural occluder and private screenshot to identify the covering layer. Do not retry Save or call the absence of a Source proof that the original click was never dispatched.",
  "runtime": "Root ran the first read-only diagnostic as source-workspace-003. The geometry/occluder extension has not been run. This lane launched no browser, test, or build."
}
