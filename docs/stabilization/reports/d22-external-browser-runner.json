{
  "schema_version": 1,
  "defect_id": "EXT-D-0022",
  "status": "read_only_recovery_prepared_source_review_required",
  "runner": "tests/browser/source-workspace-acceptance.mjs",
  "mode": "owned isolated Chrome-for-Testing profile via receipt-verified native side-panel harness",
  "authorization": "For this new isolated profile only, root authorized selecting the same previously approved existing test organization through the real Settings picker after real UI admin sign-in. The earlier primary-profile prerequisite remains unchanged. No organization is created, and another existing organization is used only for a read-only comparison before restoring the approved one.",
  "private_inputs": [
    "test-results/d22-private-config.json must be a regular mode-0600 file with approved_organization_name. The name is read at selection time and never written to the public report.",
    "At most one Save is permitted. A definitive not-saved result requires both the real Scrape UI state and one observed 200 response from its own processed_documents recognition request, scoped to the approved organization and exact public URL, with an empty result. Example is checked first, then IANA only if Example is definitively saved. An absent, duplicate, refused, or ambiguous request stops without a write.",
    "The absent-at-entry test-results/d22-source-save-attempt.json is created with exclusive mode-0600 open and fsynced with its parent directory before the trusted Save click. An existing checkpoint selects read-only recovery and never permits another Save. After a definitive real UI Source link, only that private checkpoint may atomically receive the observed Source ID."
  ],
  "evidence": "Run 001 entered one trusted Save click after a durable reservation, but the helper threw and its error was discarded. Because that can occur before pointer dispatch, on press, or on release, creation is ambiguous. The immutable source-workspace-001 receipt preserves the original observation. With its existing checkpoint, the revised runner performs real admin UI sign-in and selects the exact approved organization, resolves only the checkpoint-hashed public fixture, observes one organization- and identity-scoped 200 GET plus the matching Source recognition UI, and obtains its Source ID through the real Open link. If definitive, it continues read-only comparison-organization and restoration checks. If absent or unknown, it stops without saving. It never treats the earlier reserved-attempt counter as proof of a dispatched click. Future trusted-click failures record only allowlisted pointer failure categories and non-sensitive counts. Output omits credentials, organization names and IDs, and Source IDs.",
  "limits": [
    "Root-owned browser run 001 entered one Save attempt and exited unverified at the click helper. Its private checkpoint remains present; it must never be reset, removed, or used to authorize another Save. This lane has not launched another browser or test.",
    "The optional immediate-clear check can only pass if the Scrape pane remains mounted and observable while Settings is selected; otherwise it remains unverified rather than inferred from the later settled state.",
    "The existing real-UI sign-in scripts are executable entrypoints with no exported helper. This runner mirrors the narrow proven login sequence from isolated-admin-signin-acceptance.mjs to avoid launching a second browser through import; a later shared-helper extraction may remove this duplication."
  ],
  "next_action": "Independent source peer review, then root-owned guarded read-only recovery. Preserve the original output, receipt, and private checkpoint. No second Save is authorized."
}
