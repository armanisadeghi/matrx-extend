{
  "schema_version": 1,
  "task_id": "permission-denial-next",
  "lane": "implementation Sol medium",
  "status": "source-ready-runtime-unverified",
  "case_id": "EXT-F-1003-T57",
  "runner": "tests/browser/isolated-permission-denial-acceptance.mjs",
  "scope": "First fresh native Chrome refusal for Page archive (MHTML) in real admin Settings, using an owned disposable headed profile and the existing receipt-verified side-panel harness.",
  "source_basis": [
    "The existing isolated admin sign-in runner proves the real web-form and extension Settings Sign in path without injected sessions.",
    "The current native harness owned profile and release receipt safeguards are reused. Its new headed option changes only Chrome launch mode; headless remains the default.",
    "CUA documentation supports binding a macOS app by path and inspecting its accessibility tree/screenshot, but Chrome for Testing prompt visibility has not yet been observed in this profile."
  ],
  "acceptance": [
    "Before the request, real Settings shows admin@admin.com and a declared Page archive switch OFF; Chrome permissions.contains and getAll both show pageCapture absent.",
    "The runner sends a trusted mouse click to that actual Settings switch, then pauses. An independent UI operator uses CUA on the owned Chrome for Testing app to observe the native prompt, click Deny, and write only the word denied to the displayed acknowledgement file.",
    "After the acknowledgement, the runner requires the real Settings refusal alert, switch OFF, permissions.contains false, and getAll absence. It never mocks permissions.request, seeds grant state, or calls a Chrome permission override."
  ],
  "command_after_root_resource_admission": "node tests/browser/isolated-permission-denial-acceptance.mjs",
  "safety": "The browser profile is new and disposable; no primary Chrome profile or stored grant is touched. The runner reads only authorized test credentials at the real web login form and suppresses raw browser/auth errors. It records booleans and receipt metadata in test-results. No archive is created.",
  "runtime_gate": "Root must authorize one headed launch after fresh source peer review and resource admission. The UI operator must confirm the selected Chrome for Testing window belongs to the new owned profile before clicking Deny. If CUA cannot access that prompt or Chrome grants without prompting, leave the case UNVERIFIED and record the observed reason.",
  "verification": [
    "node --check tests/browser/isolated-permission-denial-acceptance.mjs: pass",
    "node --check tests/browser/native-sidepanel-qa-harness.mjs: pass",
    "git diff --check: pass",
    "Native Chrome denial: not launched under this lane"
  ],
  "limitations": [
    "The operator acknowledgement attests to native prompt observation and Deny; the runner independently verifies Chrome permission state and visible product feedback afterward.",
    "This first run covers pageCapture denial only. tabCapture may differ and remains separately unverified until exercised.",
    "Current CUA inventory showed Google Chrome but no running Chrome for Testing; binding to the newly launched owned window remains to be confirmed in the admitted run."
  ]
}
