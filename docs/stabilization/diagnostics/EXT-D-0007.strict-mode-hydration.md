# EXT-D-0007 — Strict Mode persisted-auth hydration red diagnostic

This is a deliberately non-gating diagnostic for the open authentication
hydration defect. The executable reproduction lives beside this note as
`EXT-D-0007.strict-mode-hydration.test.tsx`; it is outside Vitest's configured
test roots, so a known red proof cannot make an unrelated release look green
or block release gating.

To reproduce against a prospective fix, temporarily run the file from a
Vitest-included test path (without changing its assertions):

```sh
pnpm -s exec vitest run src/hooks/use-auth.strict-mode.test.tsx --maxWorkers=1 --minWorkers=1
```

On `origin/main` at the diagnostic integration audit, both persisted profiles
failed after the Strict Mode replay:

- `admin@admin.com`: expected user id `7d945376-2ac6-442c-bfb5-2ca3512377af`, received `undefined`.
- `test@test.com`: expected user id `570d5d83-2185-4dd4-80c4-3895c9e4a85f`, received `undefined`.

The open defect record is `docs/stabilization/defects/EXT-D-0007.json`. A fix
must move this exact reproduction back into an included test root and make it
green; it must not weaken or delete the assertions.
