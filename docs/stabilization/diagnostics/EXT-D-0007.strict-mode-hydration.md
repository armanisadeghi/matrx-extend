# EXT-D-0007 — Historical Strict Mode hydration red diagnostic

This file and its neighboring `.test.tsx` are preserved as the original,
non-gating red diagnostic from before the hydration repair. The executable
copy is archived under `docs/stabilization/diagnostics/`; it is outside the
Vitest include roots in `vitest.config.ts`, so normal unit-test and release
discovery does not execute it. Do not move, run, delete, or treat this archived
copy as the canonical regression test.

The active regression lives at
[`src/hooks/use-auth.strict-mode.test.tsx`](../../../src/hooks/use-auth.strict-mode.test.tsx).
It retains the original admin and ordinary-user persisted-profile Strict Mode
assertions and adds coverage for verified-bearer matching, recoverable guest
states, concurrent consumers, and stale admin state. Its active assertions are
the source of current unit evidence.

Canonical status and history remain in
[`docs/stabilization/defects/EXT-D-0007.json`](../defects/EXT-D-0007.json).
The source repair and engineering verification do not complete final native
acceptance: the current extension still needs the latest-build Chrome
observation. See the defect record and its linked runs for current evidence;
older reports that describe the defect as open or still in repair are historical
at their recorded source revisions.
