# Matrx Extend stabilization — operating plan

Status: pre-execution plan, 2026-09-25. No product tests, installation changes, or fixes have been performed by this campaign. Owner: the primary Codex chat; lane: `extend-stabilization`.

## Mandate and scope

Arman's instructions in this chat govern: organize a flat testing taskforce; inventory before any testing; exhaust every non-Chat/non-Pilot control across guest and signed-in modes; fix, independently retest, integrate, and ship continuously; synchronize remote main; protect machine resources and secrets. Chat and Pilot are last. No claim of 100% health while an applicable cell is unverified or a defect remains open.

The requested first deliverable is this plan. Read-only reconnaissance and independent plan review are preparation, not product testing. The execution checkpoint is delivery of the reviewed plan and consolidation of genuinely human-only blockers. Existing authorization covers routine development, installation switching, testing, commits, pushes, and releases; no additional general permission request is needed.

Reference standards: Chrome's own extension development/lifecycle documentation for runtime truth; WXT's documented development workflow for build identity; the user's explicit traceability and independent-verification requirements for quality. User instructions take precedence over generic campaign doctrine: no planner-only fleet, no red-trunk pushes, no mandatory owner testing gate, no extra scaffolding before the first verified fix.

## 0. Bounded baseline, then the first fix

Budget after execution starts: 90 minutes for baseline plus inventory. At 45 minutes, report concrete remaining inventory/environment gaps and narrow discovery assignments. At 90 minutes, stop adding process: finish only the minimum missing inventory/access work. Never pretend an incomplete census is complete to meet the clock. Target the first independently retested, green, pushed defect fix within the next 30 minutes; this is a target, not an invented guarantee that a defect has already been found. Record a missed target and its concrete cause. No dashboard, new platform tables, elaborate reporting framework, or expanded automation before that fix lands.

1. Read existing review feedback and active ownership, inspect main and fetch origin/main, reconcile safely before any edits. Reuse existing tests and scripts. Record baseline SHA, dirty paths, dependency lock hash, runtime/tool versions and resource measurements. Do not discard other writers' work or create worker branches/worktrees.
2. Inspect the installed Chrome extension and preserve local-only user state before any uninstall. Default to disabling the Store copy while using unpacked development, because this removes competing runtime behavior without erasing local data; remove the Store copy only after preservation is verified and any required irreversible-action confirmation is handled. Arman explicitly allowed a better setup than immediate removal. Never copy login tokens into test fixtures or committed state.
3. Use the repository's WXT development command and actual generated dev directory. Confirm the loaded extension ID, source directory, manifest, runtime build fingerprint and dev-server readiness in Chrome. Source defines development ID `cihdmkcdjjckfhjpgoedmgfpoljebaml` and Store ID `hnfolienncfklkgmdjjmhhegglimlamg`; actual installation is still unverified. Check existing auth callback/origin registrations against the observed development identity. Do not change security settings based on stale documentation.
4. Use separate persistent guest and signed-in test profiles where supported; only one runs a test at a time. Guest is genuinely signed out, without inherited browser/extension sessions. Signed-in starts with admin@admin.com. Inventory ordinary-member and organization-state differences explicitly; admin success cannot prove ordinary-member access. Keep the user's personal browsing out of disposable test fixtures. Test the actual side panel, not only an equivalent page opened in a tab.
5. Census the complete product from source navigation, nested controls, dialogs, commands, context menus, content-page entry points, settings, auth gates, tool registry and existing feature procedures. Reconcile source controls with actual guest/admin/member visibility, runtime flags, permission/desktop gates and the live active `tool.binding` advertisements for executor `chrome-extension`; document mismatches and keep inaccessible portions explicitly incomplete. Read-only UI observation may discover controls, but no behavior verdicts are recorded until the complete inventory is reconciled. A second agent reconciles the census independently. Include Chat/Pilot in inventory immediately, marked deferred.
6. Implement only the minimum launch semaphore/resource gate and durable records needed to test safely. Then test the smallest contained surface, capture the first reproducible defect, fix its class, independently retest, commit and push green. If that surface is healthy, continue to the next; never manufacture a bug to hit a deadline.

## Build and reload contract — included in every procedure

Every test and retest begins with a resource permit and a build receipt: source SHA plus dirty-source hash when applicable, lockfile hash, output hash, WXT mode, extension runtime ID, Chrome version, profile label, auth/role/org state, permissions, backend identity, fixture IDs, and timestamp. No secrets in receipts.

Receipts also declare login method, principal provenance, real data origin, build/env flags, response interception, mocks and seeded browser state. A real controlled test page is allowed and identified. Mock-backed/component/unit results are supporting evidence only; closure requires actual UI sign-in and real backend reads/writes without mocked service responses or injected sessions standing in for the authentication flow. Verify the effective running configuration rather than trusting a worker's label.

Wait for build completion; reload the extension when background, manifest, permissions, config or environment changes require it; reopen extension pages and the side panel; reload controlled target tabs to replace injected content scripts. Treat offscreen and service-worker contexts separately. Verify their running build before acting. HMR alone is not acceptance proof. Never use manifest version alone to identify development builds.

MV3 service workers may terminate when idle. Exercise restart/persistence with DevTools closed, since inspection can keep the worker alive. Separate normal idle restart from crash evidence. Confirm offscreen recreation and message routing for the surfaces being tested. Include warm run, cold/reloaded run, page navigation and relevant permission-denied/offline recovery cases. Record a lifecycle fault only after the correct rebuild/reload sequence.

Independent closure retests also use a fresh unpacked non-HMR build. Record build mode so dev-server behavior cannot hide production lifecycle defects. Release builds and dev builds must not race over output directories.

Sources: https://developer.chrome.com/docs/extensions/get-started/tutorial/debug ; https://developer.chrome.com/docs/extensions/develop/concepts/service-workers/lifecycle ; https://wxt.dev/guide/essentials/config/browser-startup .

## One inventory, linked durable evidence

Home: `docs/stabilization/`. `inventory.json` is the only authoritative scope/coverage register. Supporting records contain evidence, not competing feature status. Coordinator alone merges canonical status; workers write uniquely owned evidence/report files. All authored tracking files are committed and pushed frequently.

Each feature has stable ID `EXT-F-0001` (never renumbered/reused), parent feature if any, name, owning surface, entry points, individually identified controls/actions, source anchors, applicability rules with evidence, dependency class, wave, owner, case IDs, and explicit mode cells. IDs are allocated centrally in disjoint ranges.

Required mode axis: guest, signed-in admin, and signed-in ordinary member wherever behavior differs. Device org unset/set/switching, role, permission, session-expired and relevant service-availability states are dimensions of cases, not substitutes for auth modes. Each feature × mode cell tracks applicability (`applicable|not-applicable` plus reason), test status (`unverified|running|pass|fail|blocked|stale|not-applicable`), linked run IDs, linked defect IDs, retest status and last verified build. Terminal verdicts are pass, fail or evidenced not-applicable; blocked/unverified/stale are visibly unfinished. A denied or hidden feature still gets a negative access case where needed; not-applicable never excuses an access-control check.

Build channel is an additional case dimension: development-only controls and Store-omitted entry points must be identified explicitly. Local-development success is not a claim that a control exists in the Store package.

For org-scoped data, pair an authorized positive control with a real second-organization/non-member denial through the actual product/data door. Switching two organizations as the same all-access admin is not a non-member check. Missing suitable test principals or memberships leaves that boundary unverified and enters the consolidated access checkpoint; never weaken permissions to make a test run.

`defects/EXT-D-0001.json`: stable ID, feature ID, control/case ID, surface, auth mode, role/org/permissions, preconditions, precise numbered reproduction, expected, actual, sanitized evidence references, build receipt, reproducibility count, severity, priority, state, owner, root cause, sibling census, fix commit(s), original-repro retest run, surrounding-case runs, reviewer identity and transition history with timestamps/reasons.

Mandatory defect path: `open → triaged → in-fix → fixed → retest-pass → closed`; failure goes `fixed → retest-fail → in-fix`. Regression after closure appends a reopen event and returns to open/triaged; history is never overwritten. Duplicate/invalid reports keep an audit disposition and canonical link, never masquerade as a fixed defect. A report missing self-contained reproduction is returned for completion and stays open/untriaged. Evidence-backed intermittent faults remain open investigations; record attempts/frequency instead of throwing them away.

Severity measures user impact: S0 security/data loss or global outage; S1 a core action impossible; S2 material malfunction with workaround; S3 limited visual/usability defect. Priority independently sets order: P0 unblocks safe testing/global health; P1 current wave; P2 later contained/dependent wave; P3 deferred Chat/Pilot. A high-severity Chat/Pilot report is logged immediately and only hotfixed early when necessary for safe operation or to unblock the rest; document every exception.

`runs/<run-id>.json` stores case outcomes, timestamps, receipts, resource admission, evidence paths and contamination/invalidation reasons. `reports/<task-id>.json` stores compact worker returns. `COORDINATOR.md` stores the restart procedure, current next actions, active agent IDs/ownership, unresolved blockers, and pointers. It does not duplicate coverage counts. `events.jsonl` stores meaningful transitions, integration changes and coordinator rulings. Raw credentials, browser profiles, cookies and unredacted logs/screenshots stay out of Git.

Coverage is generated exclusively by walking inventory cells and their linked cases/defects/runs: report counts and percentages for pass/fail/unverified/blocked/stale/not-applicable separately, per surface and auth mode. Denominator is every applicable cell, not just executed tests. Passing a feature requires every required control/case to pass. A report validator checks unique IDs, foreign keys, state transitions, required evidence and orphan work. New controls discovered later extend the inventory before execution; no work disappears outside it.

## 1. Dependency-ordered stabilization

Wave A: extension-local navigation and controls, local settings/state, inspection and browser-local utilities. Choose actual surface names/order from the census, not assumptions.

Wave B: remaining non-Chat/non-Pilot surfaces with direct persistence, organization/auth behavior, saved data and external integrations. Stabilize shared prerequisites before dependent controls. Treat each surface as a bounded unit and finish all its mode/control cells before moving on, except when a genuine external dependency allows independent work elsewhere.

Wave C: non-Chat/non-Pilot paths requiring desktop/web/backend coordination. Their local UI and refusal behavior can be covered earlier; systematic cross-system end-to-end work follows the contained baseline. Logging a dependency does not remove it from the non-chat completion gate.

Wave D: Chat and Pilot only after every applicable non-Chat/non-Pilot cell passes, all their defects close, fresh-build regression is green, and release verification is recorded. If an external dependency prevents that, the gate stays visibly unmet; do not silently redefine 100% to begin Chat/Pilot. All deferred work remains inventoried.

Every surface gets exhaustive control/mode breadth, relevant adversarial boundaries, independent fix re-verification, and a final clean-build/release sweep. Optional owner feedback is welcomed but never required to keep the engineering loop moving.

## 2. Flat team and objective escalation

Maximum concurrent agents: owner plus three workers (actual platform limit). No worker creates managers, reviewer children or new chats. Owner dispatches a fresh independent reviewer. Browser/compute caps below are stricter than agent count.

| Lane | Model/effort | Work | Escalation threshold |
|---|---|---|---|
| Structured execution | GPT 6 Luna / medium | Narrow inventory slices, test batches, precise reproduction, independent straightforward retests | Two unusable/failed attempts, or 15 minutes without usable evidence: decompose once or route to Sol |
| Implementation | GPT 6 Sol / medium by default, low only for mechanical edits | Root-cause debugging, code, meaningful regression guard and self-verification | Two unsuccessful repair/retest rounds, second recurrence of the same regression, or 30 minutes without a verified increment: fresh Sol decomposition or Astra escalation |
| Exceptional escalation | GPT 6 Astra / low or medium | Hard cross-system issue or a recorded failed lower-tier attempt | One bounded 30-minute diagnostic assignment; return evidence and a concrete next move, never recursively expand the team |

Any non-building/non-running return is rejected immediately and consumes one failed attempt; it cannot advance to review. An infrastructure failure invalidates the affected run and routes environment repair, not a guessed product defect. Budgets are configurable in the campaign policy; start with time limits because live token accounting may be unavailable. When available, capture actual token usage; do not claim a spend limit is enforced without a meter. Owner checks elapsed time and last artifact before additional dispatches; third failed review round is a circuit breaker requiring a changed hypothesis or scope split.

Each task names one owned slice, acceptance criteria, applicable modes, shared-resource permit requirements, report path and allowed files. Implementation return schema: `task_id, lane, model, status, feature_ids, defect_ids, summary, changed_files, commit_shas, verification[{case_id, run_id, verdict, evidence}], limitations, open_questions, next_action`. No raw transcripts. Full evidence lives on disk; chat return is at most 15 lines.

Builder self-verification is required before return. A fresh peer verifies the original reproduction AND surrounding cases in the UI. Review has separate behavior and code-quality verdicts; both must pass. Luna handles straightforward checks; Sol handles complex code/contract review. The owner receives this compact reviewed package and integrates it. The owner does not accept a builder's own assertion as closure.

Every user-visible fix updates `docs/feature-tests.md`; every shipped capability/state change updates the canonical extension state in common-docs. Reuse and link existing review-queue items; file independently verified UI work under the campaign lane without making owner feedback a prerequisite for continued engineering.

## 3. Resource admission and watchdog

Conservative initial policy: ONE active browser test run/profile, ONE heavy job total (browser suite, build or test suite), and at most one separate lightweight read-only/code review worker while that job runs. Existing unrelated machine load counts in resource admission. Workers never launch tests directly; all campaign launch paths require the same atomic host-local permit. Machine locks/leases are outside Git; policy and receipts are versioned.

For manual Chrome runs, `--profile-dir` must point to the actual browser user-data directory (on this host, `/Users/armanisadeghi/Library/Application Support/Google/Chrome`), never the repository as a substitute. For isolated runners, pass their actual temporary-profile volume. Use the guard's `statfs` available blocks; `df` may report a different APFS availability figure and cannot override a refusal.

Before launch, sample memory pressure, available-memory estimate, CPU load, swap growth and disk space on the repo/profile volumes. Refuse admission if memory pressure is not normal, available memory is below max(4 GiB, 20% physical RAM), mean sampled CPU busy over the full 60-second window is at least 75%, or mean busy is at least 60% and ANY sample in that window has one-minute load at least 75% of logical CPUs, free disk is below 20 GiB, swap grows over 256 MiB in 60 seconds, another heavy permit is active, or a required measurement fails. These are adjustable documented operational defaults, not product limits. On macOS use appropriate pressure/VM counters, not raw free pages alone.

Watch every 15 seconds during a job. Two consecutive unsafe samples block all new launches and ask the active owned runner to stop at a safe boundary; preserve diagnostics and mark affected verdicts invalid-resource, never product-fail. Resume only after three healthy samples and a fresh preflight. Atomic acquisition plus PID/start-time/run ownership prevents races. Reclaim a stale run lease only after confirming its owning process is gone and completing ownership-token cleanup across all process sessions. Failed cleanup or process measurement retains the lease; rerunning the guarded command retries stale cleanup automatically. Never kill unrelated processes or delete another contributor's state. An initial refusal is not permission to quietly lower thresholds.

Launch contract: `scripts/stabilization-resource.mjs run` admits only the concrete commands in `resource-policy.json`, plus positional test-file filters on its single-worker Vitest command. Install uses `--ignore-scripts`; preparation is a separately admitted WXT command. These trusted tools and their descendants must preserve `MATRX_RESOURCE_OWNER`; environment scrubbing, broker/daemon handoffs and arbitrary shell commands are forbidden. Process groups alone do not contain `setsid`: an inherited-token census covers discoverable detached tool sessions, and no launched run releases its permit until that census and group cleanup succeed. Daemonizing commands are forbidden. macOS hides some protected system-binary environments; those children must remain in the foreground process group. Arbitrary detached protected binaries or hostile descendants are explicitly outside the admitted trusted-command contract. This is cooperative trusted-tool ownership, not an OS sandbox. A WXT dev server holds the single permit until stopped; use a finished unpacked development build and stop the server before obtaining the manual-browser permit. Browser work stops before the holder receives Ctrl-C. If its holder died, an agent that has stopped manual UI work can retire its stale lease with `recover-browser --confirm-no-owned-work yes`; a live holder cannot be retired. Never bypass a retained lease by deleting its files.

## 4. Trunk integration and shipping

One coordinator integrates and pushes; workers never race pushes. Workers make exact-path local commits for completed units if requested by their brief. One defect per code commit, message containing its defect ID; shared root causes use one canonical defect with linked cases. Inventory/report-only commits may be separate and still reference the relevant IDs. Never commit credentials or unrelated work.

Fetch at each task boundary and at least every 15 minutes while active; reconcile/pull remote main before each push. Fast-forward when possible; when local commits and remote have diverged, merge safely and preserve both sides. No dirty-tree rebase/autostash, reset, force push, blanket staging, or discarding concurrent changes. Resolve conflicts and retest affected behavior before pushing.

The integration barrier validates the exact candidate SHA/source fingerprint: compile, appropriate regression tests, required repository guards, independent UI retest and evidence validation. Re-read HEAD/source fingerprint before push. If remote or local source moves, invalidate affected evidence, integrate and rerun relevant checks. Retry a rejected push only after reconciling. Never label a different SHA green or push red trunk; if inherited trunk is red, repair the blocking baseline rather than weakening gates.

Push verified code and sanitized records frequently. Refresh @ai-matrx packages to latest and reconcile consumer actions before release as required by this repo. Source inspection found a concrete release mismatch: `release.sh` generates/bump/merges a candidate and pushes/tags BEFORE checks, treats failed checks as findings, and runs checks/build in parallel. Checking the earlier checkout SHA cannot certify that later candidate. The user's current never-red-trunk instruction supersedes the script's older push-first comments.

Before the first campaign release, amend the release path so the final regenerated/version-bumped/merged candidate is checked and packaged before any branch/tag publication, under the same single-heavy-job admission policy. A changed remote candidate must be reconstructed and revalidated before retry; a failure must leave publication untouched. Verify this behavior with local disposable Git remotes and deliberately failing checks, independently, after inventory permits test execution. Preserve concurrent source changes and do not silently ship only origin when local conflict resolution failed. This is execution work to inventory and track, not a claim that the script is already fixed.

Run the repaired repo release path after a verified batch. Pause the owned dev server and browser tests before release; serialize checks and packaging, then promote output only after they pass. Restart dev, rebuild/reload, verify runtime fingerprint and retest before resuming. The dev server and release must never write `.output/chrome-mv3-dev` concurrently. Normally batch a day's release, with earlier release for a critical repair. Do not confuse Git push, unpacked build, release artifact and Chrome Web Store publication. Record each independently. Rebuild/reload and smoke the released unpacked artifact; Store provider review stays explicitly pending until verified. Known historical publisher-verification issues are leads to recheck, not assumed current blockers.

## 5. Consolidated anticipated blockers and defaults

| Area | Current evidence / uncertainty | Default resolution and closure proof |
|---|---|---|
| Chrome installation/control | Chrome and a connected browser surface are available; installed Matrx channel not yet inspected | Inspect details, preserve local state, disable Store copy, load unpacked, verify real side panel/runtime ID |
| Identity/auth callbacks | Source carries distinct dev/Store callback identities | Verify existing origin/callback configuration and perform real sign-in on the dev identity; investigate configuration before calling it a product fault |
| Vault and test identity | User authorizes vault access and admin account; no dedicated Matrx vault connector is exposed in this session | Use the authenticated Matrx UI through available browser control; credentials only at point of use. Existing authorized local secret source is a fallback; never print values |
| Ordinary-member coverage | Admin-only gates exist; non-admin test identity and memberships not yet verified | Find an existing authorized test identity; otherwise leave those cells explicitly unverified and report the exact missing access at the checkpoint. Do not infer ordinary-user health from admin |
| Organization/test data | Admin organization choice and safe mutation targets not yet verified | Use an explicitly selected device organization and realistic campaign-owned disposable records; preserve existing user records; verify create/edit/archive/restore by rereading |
| External effects | Email, provider accounts, downloads, optional permissions and desktop actions may exist | Inventory all of them. Use owned test targets and existing grants; no unsolicited messages to real people, paid actions, permanent data deletion or broader security grants. Any unavailable real end-to-end case stays unverified, never replaced by a fake pass |
| Runtime dependencies | Backend, DB, desktop engine and provider availability not yet checked | Repair locally authorized prerequisites, record service identities, separate dependency outages from extension failures, progress independent surfaces |
| Remote concurrency | Local main currently clean and tracking origin/main; remote freshness not yet verified | Coordinator fetch/pull and serialized green integration; preserve concurrent contributors' intent |
| Resources | Exact sustainable capacity being assessed | Single-heavy-job admission gate; refuse unsafe runs and repair environment first |
| Ambiguous behavior | Existing docs contain historical claims that may conflict | Current user mandate, current canonical contracts, then observed code/data; coordinator records reversible decisions and expected behavior before tests |
| Shipping | Current release script pushes before checks and runs heavy work in parallel; historical Store verification hold is documented | Repair exact-candidate publication ordering and serialize resource use before release; distinguish local artifact health from Store publication; human/provider-only step never prevents other fixes |
| Continuity | Files and agent tools support resumable active work; no persistent wakeup is established | Keep working while the chat is active, drain workers before stopping, checkpoint every unit. A named recurring wakeup needs a concrete interval/registration under repository policy; do not imply a background daemon exists |

No human-only blocker has yet been proven. Unverified access is not evidence of a blocker. Resolve discoverable facts during the capped baseline, then present any remaining human-only actions together in one exchange. Routine engineering choices are delegated already. Future unexpected human/provider gates affect only their own cases; continue all independent work and keep the gap visible. No promise can eliminate a future provider CAPTCHA, changed access or account verification.

## Preparation evidence and plan review

Read-only source reconnaissance on 2026-09-25 found main at `aac7be5` (then equal to the local origin/main tracking ref, not a fresh remote verification), 16 GiB RAM, about 1.6 TiB available disk, and substantial concurrent indexing/renderer CPU. Source lists side-panel domains including tasks, lists, agenda, scrape, saved captures, capture, data, highlights, guidance, SEO, notes, files, screenshots, vault, tools, settings, profile, showcase, broker and debug, plus deferred Chat/Pilot. This is a preliminary domain census, NOT the complete feature inventory and NOT a test result. WXT dev port is 3025. Chrome control is available; installed extension and actual side-panel behavior remain unverified.

Independent Sol/medium REGRET and BUILDABILITY reviews mapped all requested requirements and raised five material findings. All accepted and addressed here: runtime/DB-gated inventory reconciliation; real-auth/data/mock provenance; second-org/non-member negative controls; exact-candidate pre-publication checks; serialized release work and exclusion of dev-output races. Reviews were source/read-only observations, not product verification. No human-only question was established. No product tests/builds/install changes were run for this planning deliverable.

## Resuming and completion

Resume by reading the current user mandate, this plan and COORDINATOR, checking active worker/process ownership, fetching remote, loading inventory and linked records, and selecting the next unresolved cell. Never redispatch completed work based only on a lost chat summary. Changes to covered controls invalidate linked cases as stale; new remote features join the census. Unknown impact triggers a broader retest, never optimistic preservation of green.

Completion means every current feature/control/mode has a terminal evidenced verdict, all applicable non-deferred cases pass, defects have independent original-repro and surrounding-case proof, exact integrated build gates pass, source/artifacts are pushed, and release status is truthful. Full project health additionally requires the final Chat/Pilot wave. Report tested scope/build and unresolved external actions rather than an unqualified claim of permanent bug freedom.
